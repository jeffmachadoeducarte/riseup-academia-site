node_modules/
.next/
out/
build/
.DS_Store
*.pem
.env
.env*.local
npm-debug.log*
yarn-error.log*
next-env.d.ts
.vercel
*.tsbuildinfo

# Pasta original do cliente. O mesmo arquivo está versionado em
# assets/source/rise-up-reel-original.mp4, que é a fonte do pipeline de mídia.
# Fica fora do Git para não duplicar 15 MB — o arquivo segue intacto em disco.
reels/

# Banco SQLite da área do aluno. Em produção vive num volume do EasyPanel
# montado em /app/dados — nunca no repositório.
dados/
